mutation LoginWithCredentials($input: LoginWithCredentialsInput!) {
    loginWithCredentials(input: $input) {
        __typename

        ... on LoginSuccess {
            userId
        }

        ... on LoginFailure {
            reason
        }

        ... on PendingLink {
            allowedProviders
        }
    }
}